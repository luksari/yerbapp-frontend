import React, { FC } from 'react';
import { LogoContainer, StyledLogo } from './styled';


const Logo: FC = () => {
  return (
    <LogoContainer>
      <StyledLogo />
    </LogoContainer>
  );
};

export default Logo;
