import React, { FC } from 'react';
import { LogoContainer, StyledLogo } from './styled';


export const Logo: FC = () => {
  return (
    <LogoContainer>
      <StyledLogo />
    </LogoContainer>
  );
};
